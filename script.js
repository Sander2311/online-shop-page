const btn = document.querySelectorAll(".btn-for-buy");


btn.forEach(element => {
    element.addEventListener("click", f = () =>{
        window.open('https://www.instagram.com/kalina_p11/');
    })
});